[project]
name = "dify-plugin-caldav"
version = "0.0.1"
description = "Read and create calendar events and tasks on any CalDAV server"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.9.0",
    "caldav>=3,<4",]

[dependency-groups]
dev = []
